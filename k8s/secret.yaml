apiVersion: v1
kind: Secret
metadata:
  name: preview-worker-secret
  namespace: preview
type: Opaque
data:
  # IMPORTANT: Replace with 'echo -n YOUR_SECURE_TOKEN | base64'
  auth-token: REPLACE_ME_WITH_BASE64_TOKEN
